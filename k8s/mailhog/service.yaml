apiVersion: v1
kind: Service
metadata:
  name: mailhog
  namespace: splitease
spec:
  selector:
    app: mailhog
  type: NodePort
  ports:
    - name: smtp
      port: 1025
      targetPort: 1025
    - name: http
      port: 8025
      targetPort: 8025
      nodePort: 30825     # access the web UI at minikube-ip:30825
