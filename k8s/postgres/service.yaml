apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: splitease
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
  clusterIP: None   # headless — StatefulSet pods get stable DNS names
